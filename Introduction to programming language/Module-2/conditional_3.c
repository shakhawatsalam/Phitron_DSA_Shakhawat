#include <stdio.h>

int main()
{
    int tk;
    scanf("%d", &tk);
    if (tk >= 5000)
    {
        printf("Cox's Bazar Jabo\n");
        if (tk >= 10000)
        {
            printf("Sent martin jabo\n");
        }
        else
        {
            printf("Ferot Chola asbo");
        }
    }
    else
    {
        printf("kothaw jabo na");
    }
    return 0;
}

// Epic Pen