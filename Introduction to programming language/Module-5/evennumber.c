#include <stdio.h>

int main()
{
    // int N;
    // scanf("%d", &N);
    // int L = N / 5 - 4;
    // for (int i = 0; 9 >= i; i += 2)
    // {
    //     printf("%d ", L + i);
    // }
    int N;
    scanf("%d", &N);
    int L = N / 4 - 3;

    for (int i = 0; 7 >= i; i += 2)
    {
        printf("%d ", L + i);
    }
    return 0;
}