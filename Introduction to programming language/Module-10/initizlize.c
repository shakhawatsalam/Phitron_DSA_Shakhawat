#include <stdio.h>

int main()
{
    char a[] = "Shawon";
    // printf("%d", sizeof(a));
    printf("%s", a);
    // for (int i = 0; i < 6; i++)
    // {
    //     printf("%c\n", a[i]);
    // }
    return 0;
}